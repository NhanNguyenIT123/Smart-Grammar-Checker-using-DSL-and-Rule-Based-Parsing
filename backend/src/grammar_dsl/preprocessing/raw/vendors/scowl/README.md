# SCOWL Vendor Drop

Place the downloaded SCOWL word list here.

Recommended input:
- a plain-text word list such as `english-words.60`

Import command:

```powershell
python backend/run.py import-scowl --input backend/src/grammar_dsl/preprocessing/raw/vendors/scowl/english-words.60
```
